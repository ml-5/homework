//方法1:
//char *strstr(char *string, char *pattern)
#include <stdio.h>
#include <stdio.h>
#include <string.h>
typedef char *Position;
int main()
{
    g[] p p char string[] = "This is a simple example.";
    char pattern[] = "simple";
    Position p = strstr(string, pattern);
    printf("%s\n", p);
    return 0;
}