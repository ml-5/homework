/**
环形队列（或循环队列）中实现队列的基本运算
rear  c 4
      b 3
front a 2
        1
        0
当采用rear==MaxSize-1,作为队满条件的缺陷，队中可能还有若干的空位
这种溢出并不是真正的溢出，称为假溢出.
解决的方案
把数组的前端和后端连接起来，形成一个环形的顺序表，即把存储队列元素的表从逻辑上看成一个环，
称为环形队列或循环队列
对于之前的操作，改变方式
将rear++ front++ 改为 rea=(rear+1)%MaxSize front=(front+1)%MaxSize
约定
rear=front为队空
(rear+1)%MaxSize==front 为队满
注意
进队一个元素时到达队头，就认为队满了。
这样做会少放一个元素，牺牲一个元素没关系的。

环形队列的四要素

队空条件：front = rear

队满条件：(rear+1)%MaxSize = front

进队e操作：rear=(rear+1)%MaxSize; 将e放在rear处 

出队操作：front=(front+1)%MaxSize; 取出front处元素e;
**/